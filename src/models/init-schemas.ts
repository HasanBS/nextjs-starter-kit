import './MenuItem';
import './Menu';

// Calling this function will initialize all schemas imported in this file. If you don't we get errors like "MissingSchemaError: Schema hasn't been registered for model "MenuItem".
export function initSchemas() {
    console.log('Initializing schemas');
}
